<section id="deatils-page">
    <div class="wrapper">
        {{#with realEstate}}
        <div class="product-img">
            <img src="./static/images/apartments.jpg">
        </div>
        <div class="product-info">
            <div class="product-text">
                <div class="product-text">
                    <h1>Name: {{name}}</h1>
                    <h2>Type: {{type}}</h2>
                    <h4>Year: {{year}}</h4>
                    <h4>City: {{city}}</h4>
                    <p>Description: {{description}}</p>

                    {{#if renters}}
                    <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                    <p>People rented this housing: {{rentersNames}}</p>
                    {{else}}
                    <!-- If not display: -->
                    <p>People rented this housing: There are no tenants yet.</p>
                    {{/if}}
                </div>
            </div>
            {{#if ../user}}
            <!-- if there is no registered user, do not display buttons-->
            <div class="product-btn">
                {{#if isOwner}}
                <!-- Only for registered user and creator of the housing offer-->
                <a href="/realEstate/{{_id}}/edit" class="edit">Edit</a>
                <a href="/realEstate/{{_id}}/delete" class="remove">Delete</a>
                {{else}}
                    {{#if isNotAvailable}}
                     <!-- logged in user with no available pieces-->
                     <p class="no-housing">No Housing Available!</p>
                    {{else}}
                    <!-- logged in user with available pieces-->
                        {{#if hasRented}}
                         <p class="alRentHome">You have already rent this home</p>
                        {{else}}
                        <a href="/realEstate/{{_id}}/rent" class="rentHome">Rent a home, available {{availablePieces}} housing</a>
                        {{/if}}
                    {{/if}}
                {{/if}}
            </div>
            {{/if}}
        </div>
    </div>
    {{/with}}
</section>